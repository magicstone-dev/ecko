# frozen_string_literal: true

RSpec.describe Ecko::Plugins::Sponsor do
  it "has a version number" do
    expect(Ecko::Plugins::Sponsor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
